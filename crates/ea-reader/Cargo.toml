[package]
name = "ea-reader"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
# Die einzige Kante dieser Crate, und sie ist der Grund fuer ihre Existenz:
# `GATE_ORDER_V1` wird RE-EXPORTIERT und nie ein zweites Mal geschrieben.
# `ea-verify` steht selbst auf der wasm32-Positivliste, `web-reader-design.md`
# §9 endet genau dort.
ea-verify.workspace = true
